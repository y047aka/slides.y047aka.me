{
  "scripts": {
    "clean": "rimraf assets/elm.js",
    "compile": "elm make src/Main.elm --optimize --output=assets/elm.optimized.js",
    "minify": "google-closure-compiler --js=assets/elm.optimized.js --js_output_file=assets/elm.js && rm assets/elm.optimized.js",
    "build": "npm-run-all -s clean compile minify",
    "start": "elm reactor"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2",
    "elm-test": "^0.19.1-revision2",
    "google-closure-compiler": "^20200204.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
